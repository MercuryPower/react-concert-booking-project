import {useMemo} from "react";
import {ConcertInfo} from "../../data";

export const useTickets = (tickets:ConcertInfo) => {
    // return useMemo(() => {
    //         return [...tickets];
    // }, [tickets])
}